---

- hosts: all
  become: true
  become_method: sudo
  remote_user: "vagrant"
  roles:
    - base

- hosts: web
  become: true
  become_method: sudo
  remote_user: "vagrant"
  roles:
    - web

- hosts: db
  become: true
  become_method: sudo
  remote_user: "vagrant"
  roles:
    - db
